vnfd:vnfd-catalog:
    vnfd:
    -   id: server-vnf
        name: server-vnf
        short-name: server-vnf
        description: Generated by OSM package generator
        vendor: OSM
        version: '1.0'
        connection-point:
        -   name: server/cp0
            type: VPORT

        # Management interface
        mgmt-interface:
            cp: server/cp0

        vdu:
        -   id: server-VM
            name: server-VM
            description: server-VM
            count: 1
            cloud-init-file: cloud-config.txt
            # Flavour of the VM to be instantiated for the VDU
            vm-flavor:
                vcpu-count: 2
                memory-mb: 2048
                storage-gb: 10

            # Image including the full path
            image: server-image

            interface:
            # Specify the external interfaces
            # There can be multiple interfaces defined
            -   name: server-eth0
                position: 0
                type: EXTERNAL
                virtual-interface:
                    type: VIRTIO
                external-connection-point-ref: server/cp0



 
